// ui/theme/Color.kt
package com.teduniversity.medicalai.ui.theme

import androidx.compose.ui.graphics.Color

// — Primary (buton, aktif durumlar)
val BrandPrimaryBlue    = Color(0xFF005A9C)
val BrandOnPrimaryBlue  = Color(0xFFFFFFFF)

// — Secondary (vurgulama, accent)
val BrandSecondaryGreen   = Color(0xFF47A964)
val BrandOnSecondaryGreen = Color(0xFFFFFFFF)

// — Arka plan
val AppBackground        = Color(0xFFF5F5F5)
val AppOnBackground      = Color(0xFF000000)

// — Kart, TextField vb. yüzeyler
val AppSurface           = Color(0xFFFFFFFF)
val AppOnSurface         = Color(0xFF000000)

// — Container rolleri (daha açık ton)
val PrimaryContainer     = Color(0xFFD6E4FF) // primary’ın pastel alt tonu
val OnPrimaryContainer   = Color(0xFF003A75)
val SecondaryContainer   = Color(0xFFD1F8DD) // secondary’nin pastel alt tonu
val OnSecondaryContainer = Color(0xFF1B5E20)

// — Outline / border
val AppOutline           = Color(0xFFBDBDBD)

// — Hata
val ErrorRed             = Color(0xFFB00020)
val OnErrorRed           = Color(0xFFFFFFFF)
